


import 'package:flutter/material.dart';

class hello_world extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(title: Text("Test Git Remote"),),
        body: Center(child: Text("Test Git Remote"),),
      ),
      
    );
  }
}